[project]
name = "easy_mcp_autocad"
version = "0.2.0"
description = "AutoCAD MCP Server for AI-driven CAD operations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.4.1",
    "pywin32>=309",
]
